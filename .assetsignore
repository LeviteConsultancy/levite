# Do not publish these as public static files (assets root is ".")
Dockerfile
wrangler.toml
.git
.gitignore
# Worker source (not public)
src/
# Let `/` be handled by src/index.js (proxy), not a static index.html
index.html
